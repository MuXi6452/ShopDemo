<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:paddingTop="?attr/actionBarSize"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Button
        android:id="@+id/btn_cart_edit"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="20dp"
        android:text="编辑"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="30dp"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/xrv_cart"
        android:layout_below="@id/btn_cart_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btn_cart_selectAll"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:text="全选"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="30dp"/>
    <TextView
        android:id="@+id/tv_cart_totolPrice"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/btn_cart_selectAll"
        android:text="合计：Y0"
        android:gravity="left"
        android:layout_marginLeft="8dp"
        android:textColor="#f4c521"
        android:singleLine="true"
        android:layout_width="200dp"
        android:layout_height="30dp"/>
    <Button
        android:id="@+id/btn_cart_calculate"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="结算"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="30dp"/>
    <Button
        android:id="@+id/btn_cart_delete"
        android:visibility="gone"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="删除"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="30dp"/>
</RelativeLayout>
